<form action="/bookings" method="post">
  <label>
  Select a class:
    <select required name="fitness_class_id">
      <% for fitness_class in @fitness_classes %>
      <% if fitness_class.is_full? then %>
      <% else %>
      <option value="<%= fitness_class.id() %>"><%= fitness_class.name %></option>
      <% end %>
      <% end %>
    </select>
  </label>

  <label>
  Select a boxer:
    <select required name="boxer_id">
      <% for boxer in @boxers %>
      <option value="<%= boxer.id() %>"><%= boxer.full_name() %></option>
      <% end %>
    </select>
  </label>

  <input class="btn btn--action" type='submit' value="Add Booking">
</form>
